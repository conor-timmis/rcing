/** Standard altar profit tab — throughput and XP modifiers (OSRS Wiki). */

const PROFIT_SECONDS_PER_HOUR = 3600;
/** Reasonable non-optimal trip time for direct altar runs (e.g. wrath ~45s). */
const PROFIT_DEFAULT_TRIP_SECONDS = 45;
/** Daeyalt essence grants 50% extra Runecraft XP vs pure essence. */
const PROFIT_DAEYALT_XP_MULTIPLIER = 1.5;
/** Seconds lost per standard pouch tier per hour without RC cape (NPC Contact). */
const PROFIT_NPC_CONTACT_SECONDS_PER_POUCH = 15;
/** RC cape method: mythical cape stays in inventory (−1 essence slot). */
const PROFIT_COLOSSAL_CAPE_ESSENCE_PENALTY = 1;
